{
  "name": "specbook",
  "private": true,
  "version": "0.1.0",
  "description": "Specbook — VS Code Extension & Web App",
  "scripts": {
    "dev": "pnpm --filter @specbook/web run dev",
    "dev:desktop": "pnpm --filter @specbook/shared run build && pnpm --filter @specbook/ai run build && pnpm --filter @specbook/desktop run dev",
    "build": "pnpm -r run build",
    "dist:desktop": "pnpm --filter @specbook/shared run build && pnpm --filter @specbook/ai run build && pnpm --filter @specbook/desktop run build && pnpm --filter @specbook/desktop run dist",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.15.4"
}